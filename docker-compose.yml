services:
  agentwish:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - agentwish_data:/data
    environment:
      - DATABASE_PATH=/data/agentwish.db
      - PORT=5000
      - API_KEY_SALT=agentwish_2024_secure_salt_v1
    restart: always

volumes:
  agentwish_data:
